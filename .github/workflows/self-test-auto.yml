name: Self-test (auto-SBOM)
on:
  workflow_dispatch:
  push:
    branches: [main]

# Read-only GITHUB_TOKEN — workflow only checks out the repo and runs the
# action against an auto-generated SBOM. No writes anywhere.
permissions:
  contents: read

jobs:
  auto:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Sign with auto-generated SBOM
        id: lp
        uses: ./
        with:
          api-key: ${{ secrets.LEDGERPROVE_API_KEY }}
          repo-id: rajibcse94/sign-sbom-auto-test
      - run: echo "Verify → ${{ steps.lp.outputs.verification-url }}"
