{
  "name": "ledgerprove-sign-sbom-action",
  "version": "1.0.0",
  "description": "GitHub Action: cryptographically sign your SBOM with LedgerProve.",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "watch": "ncc build src/index.ts -o dist --source-map --watch",
    "package": "npm run build"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "@types/node": "^20.11.0",
    "typescript": "^5.5.0"
  }
}
